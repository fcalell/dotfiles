# Hypridle Configuration
# Automatic idle management for Hyprland

general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display
}

# Turn off screen after 10 minutes of inactivity (unless a window is fullscreen)
listener {
    timeout = 600                                # 10 minutes
    on-timeout = hyprctl clients -j | jq -e 'map(select(.fullscreen != 0)) | length == 0' && hyprctl dispatch dpms off       # screen off when timeout has passed (only if no fullscreen windows)
    on-resume = hyprctl dispatch dpms on         # screen on when activity is detected after timeout has fired
}
